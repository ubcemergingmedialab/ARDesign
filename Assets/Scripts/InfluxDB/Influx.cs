namespace ARDesign
{
    /// <summary>
    /// Handles all database fetching related functions
    /// \todo Eventually we should move database stuff onto an external server, these operations are too bulky and slow for the hololens
    /// </summary>
    namespace Influx { };
}